import * as React from 'react';
import {Jumbotron} from 'react-bootstrap';

const Header = () => (
  <header>
    <Jumbotron>
      A nice header
    </Jumbotron>
  </header>
);

export default Header;
